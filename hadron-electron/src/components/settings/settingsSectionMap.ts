// Populated incrementally as section components are extracted (Phases 2–6).
// When all sections are extracted, SECTION_COMPONENTS will be a complete
// Record<SettingsSection, React.LazyExoticComponent<any>> and the shell's
// switch/conditional rendering can be replaced with a single map lookup.
export {};
