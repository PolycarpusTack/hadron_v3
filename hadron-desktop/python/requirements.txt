# Smalltalk Crash Analyzer - MVP Dependencies
# Install with: pip install -r requirements.txt

# AI Providers
openai>=1.0.0
anthropic>=0.9.0
requests>=2.31.0

# Configuration
pyyaml>=6.0

# Knowledge Base / OpenSearch integration
opensearch-py>=2.4.0

# Optional: Local AI (llama.cpp uses OpenAI-compatible API, no Python package needed)
# httpx is used for local embeddings
httpx>=0.24.0
